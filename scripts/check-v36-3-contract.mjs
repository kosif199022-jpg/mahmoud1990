import fs from 'node:fs';
const read=p=>fs.existsSync(p)?fs.readFileSync(p,'utf8'):'';
const html=read('public/index.html'),front=read('frontend/index.html'),workspace=read('src/kosif-workspace.js'),worker=read('src/worker.js'),sw=read('public/sw.js'),stdSw=read('public/standards/sw.js'),pkg=read('package.json'),cont=read('public/v36-continuity.js'),css=read('public/v36-continuity.css'),eng=read('public/v36-engagement.js'),req=read('docs/KOSIF_MASTER_REQUIREMENTS_2026-08-17.md');
const failures=[];const ok=(name,v)=>{console.log((v?'✅':'❌')+' '+name);if(!v)failures.push(name)};
const count=(s,re)=>(s.match(re)||[]).length;
ok('shell copies identical',html===front);
ok('v36.3 build endpoint',/\/__version/.test(worker)&&/2026\.08\.17-v36\.3-master-requirements/.test(worker)&&/version:'v36\.3'/.test(worker));
ok('v36.3 release header',/native-v36-3-master-requirements/.test(worker));
ok('continuity assets wired',html.includes('/v36-continuity.css?v=36.3')&&html.includes('/v36-engagement.js?v=36.3')&&html.includes('/v36-continuity.js?v=36.3'));
ok('fallback injection wires continuity',workspace.includes('/v36-continuity.css?v=36.3')&&workspace.includes('/v36-engagement.js?v=36.3')&&workspace.includes('/v36-continuity.js?v=36.3'));
const nav=[...html.matchAll(/<button data-kgo="([^"]+)"[^>]*>[\s\S]*?<\/button>/g)].slice(-4).map(m=>m[1]);
ok('mobile nav primary routes',JSON.stringify(nav)===JSON.stringify(['overview','tb','rounds','pbc']));
ok('mobile nav labels exact',/الرئيسية<\/button>[\s\S]*الميزان<\/button>[\s\S]*الجولات<\/button>[\s\S]*المطالبات<\/button>[\s\S]*المزيد<\/button>/.test(html));
ok('map excluded from mobile primary nav',!/<button data-kgo="map"/.test(html));
ok('font range reaches 200',/id="kf-range"[^>]*max="200"/.test(html)&&/Math\.min\(2,Math\.max\(\.9/.test(html)&&/90% إلى 200%/.test(html));
ok('AI helper declarations deduplicated',count(html,/function aiOwnerUnlocked\(\)/g)===1&&count(html,/function aiVerified\(/g)===1&&count(html,/function invalidateAI\(/g)===1);
ok('main client no whole-document mutation observer',!workspace.includes(".observe(document.documentElement,{childList:true,subtree:true})")&&!html.includes(".observe(document.documentElement,{childList:true,subtree:true})"));
ok('source client no key-presence Active state',!workspace.includes("wanted=k?'AI Active ·")&&!workspace.includes("p.classList.toggle('on',!!k)"));
ok('production client connected requires verification',/connected=aiOwnerUnlocked\(\)&&!!k&&aiVerified/.test(html));
ok('continuity AI uses server verified state',/KosifAIGate\?\.verified/.test(cont)&&/data\.kosifAiUnlocked|dataset\.kosifAiUnlocked/.test(cont));
ok('fixed navigation SVG geometry',/--k363-icon:22px/.test(css)&&/#kosif-bottom-nav svg/.test(css));
ok('More sheet scroll contract',/max-height:min\(88dvh,900px\)/.test(css)&&/overscroll-behavior:contain/.test(css));
ok('engagement ISA 210',/ISA 210/.test(eng));
ok('engagement ISA 220',/ISA 220/.test(eng));
ok('engagement ISQM 1',/ISQM 1/.test(eng));
ok('engagement listed PIE EQCR framework',/listed/.test(eng)&&/pie/.test(eng)&&/eqcr/.test(eng)&&/framework/.test(eng));
ok('engagement is advisory not opinion',/لا تُصدر رأيًا|advisory:true/.test(eng));
ok('app cache generation v36.3',/kosif-native-v36-3-app/.test(sw));
ok('standards cache generation v36.3',/kosif-native-v36-3-standards/.test(stdSw));
ok('API excluded from app SW',/pathname\.startsWith\('\/api\/'\)/.test(sw));
ok('legacy cache purge retained',/tamhees/i.test(sw)&&/tamhees/i.test(stdSw));
ok('package is v36.3 and contract runs',/"version"\s*:\s*"36\.3\.0"/.test(pkg)&&/check-v36-3-contract\.mjs/.test(pkg));
ok('master requirements registry exists',req.includes('Master Requirements')&&req.includes('90% through 200%')&&req.includes('ISA 210')&&req.includes('AI Agent'));
ok('no document.write architecture in primary shell',!html.includes('document.write(')&&!html.includes('theme-restore'));
const ss=worker.indexOf('async function nativeShell'),se=worker.indexOf('async function assetIndexDiagnostic'),sf=ss>=0&&se>ss?worker.slice(ss,se):'';
ok('root shell is native and fail closed',ss>=0&&sf.includes('status:503')&&!sf.includes('legacyWorker')&&worker.includes("u.pathname==='/'||u.pathname==='/index.html'")&&worker.includes('return nativeShell(req,env)'));
ok('root excluded from legacy fallback set',!worker.includes("const E=new Set(['/','/index.html'"));
ok('asset index diagnostic exists',worker.includes("u.pathname==='/__asset-index'")&&worker.includes('hasKosifBoot'));
console.log(`KOSIF_V36_3_CONTRACT ${failures.length?'FAILED':'OK'} failures=${failures.length}`);if(failures.length){for(const x of failures)console.error(' - '+x);process.exit(2)}
