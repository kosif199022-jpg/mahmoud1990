const {chromium}=require('playwright');
const base=process.env.KOSIF_TEST_URL||'http://127.0.0.1:8787';
(async()=>{const b=await chromium.launch({headless:true});const c=await b.newContext({viewport:{width:390,height:844}});const p=await c.newPage();const errors=[],logs=[];p.on('pageerror',e=>errors.push(String(e)));p.on('console',m=>logs.push([m.type(),m.text()]));await p.goto(base+'/?diag='+Date.now(),{waitUntil:'networkidle'});await p.waitForTimeout(3000);const d=await p.evaluate(()=>({bodyClass:document.body?.className||'',ready:document.body?.classList.contains('kosif-ready')||false,boot:document.querySelector('#kosif-boot')?{className:document.querySelector('#kosif-boot').className,error:document.querySelector('#kosif-boot').dataset.error||'',text:document.querySelector('#kosif-boot').innerText.slice(0,500)}:null,bottom:!!document.querySelector('#kosif-bottom-nav'),workspace:window.__KOSIF_WORKSPACE_VERSION__||null,scripts:[...document.scripts].map(x=>x.src||x.id||'inline').slice(-20)}));console.log('BOOT_DIAG',JSON.stringify(d,null,2));console.log('PAGE_ERRORS',JSON.stringify(errors,null,2));console.log('CONSOLE',JSON.stringify(logs.slice(-20),null,2));await b.close();if(!d.ready)process.exit(2)})().catch(e=>{console.error(e.stack||e);process.exit(3)});
